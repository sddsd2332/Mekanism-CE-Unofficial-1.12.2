package mekanism.common.content.qio;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

final class QIOLog {

    static final Logger LOGGER = LogManager.getLogger("Mekanism QIO");

    private QIOLog() {
    }
}
